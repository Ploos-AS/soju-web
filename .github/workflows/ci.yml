name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: '1.24.x'

      - name: Format check
        run: |
          gofmt -d .
          test -z "$(gofmt -l .)"

      - name: Test
        run: go test ./...

      - name: Vet
        run: go vet ./...

      - name: Verify release-ref policy
        run: |
          git tag m11.2-test "$GITHUB_SHA"
          if sh scripts/verify-release-ref.sh m11.2-test "$GITHUB_SHA"; then
            echo "non-SemVer tag unexpectedly accepted" >&2
            exit 1
          fi
          git tag v0.0.0-m11.2 "$GITHUB_SHA"
          sh scripts/verify-release-ref.sh v0.0.0-m11.2 "$GITHUB_SHA"
          if sh scripts/verify-release-ref.sh v0.0.0-m11.2 0000000000000000000000000000000000000000; then
            echo "tag/commit mismatch unexpectedly accepted" >&2
            exit 1
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Validate Compose
        env:
          SOJU_WEB_ADMIN_PASSWORD: ci-password-123456
          SOJU_WEB_SESSION_SECRET: ci-session-secret-01234567890123456789
        run: docker compose config --quiet

      - name: Build amd64 image
        run: |
          docker build \
            --build-arg VERSION=ci \
            --build-arg REVISION="$GITHUB_SHA" \
            -t soju-web:ci .

      - name: Verify non-root runtime
        run: |
          test "$(docker run --rm --entrypoint id soju-web:ci -u)" = "1000"
          test "$(docker run --rm --entrypoint id soju-web:ci -g)" = "1000"

      - name: Verify health endpoint
        run: |
          cid=$(docker run -d --rm \
            -e SOJU_WEB_ADMIN_PASSWORD=ci-password-123456 \
            -e SOJU_WEB_SESSION_SECRET=ci-session-secret-01234567890123456789 \
            -p 127.0.0.1:18080:8080 soju-web:ci)
          trap 'docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT
          for i in $(seq 1 20); do
            if curl -fsS http://127.0.0.1:18080/healthz | grep -qx ok; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:18080/healthz | grep -qx ok
          docker rm -f "$cid"
          trap - EXIT

      - name: Build arm64 image
        run: |
          docker buildx build \
            --platform linux/arm64 \
            --build-arg VERSION=ci \
            --build-arg REVISION="$GITHUB_SHA" \
            --load \
            -t soju-web:ci-arm64 .

      - name: Verify arm64 binary starts
        run: |
          cid=$(docker run -d --rm --platform linux/arm64 \
            -e SOJU_WEB_ADMIN_PASSWORD=ci-password-123456 \
            -e SOJU_WEB_SESSION_SECRET=ci-session-secret-01234567890123456789 \
            soju-web:ci-arm64)
          trap 'docker logs "$cid" 2>&1 || true; docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT
          started=false
          for i in $(seq 1 20); do
            if ! docker ps --filter "id=$cid" --format '{{.ID}}' | grep -q .; then
              break
            fi
            if docker logs "$cid" 2>&1 | grep -q "listening on :8080"; then
              started=true
              break
            fi
            sleep 1
          done
          test "$started" = true
          docker rm -f "$cid"
          trap - EXIT
